# Database Configuration
# PostgreSQL connection string
# Format: host=<host> user=<user> password=<password> dbname=<dbname> sslmode=<disable|require|verify-ca|verify-full>
GOOSE_DBSTRING=host=localhost user=postgres password=postgres dbname=ecom sslmode=disable

# Server Configuration
# HTTP server port (default: 8080)
PORT=8080

# Logging Configuration
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Feature Flags (optional)
# Enable detailed SQL logging (dev only, can impact performance)
DEBUG_SQL=false
